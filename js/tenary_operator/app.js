

let num = 20;

// let name = condition ? "value if true" : "value if false";

let res = num > 10 ? "Greater than 10" : "Less than 10";
console.log(res);


let inputUsername = 'John';

$username = inputUsername ? inputUsername : 'Guest';
console.log($username);


const email = 'example@example.com';
const message = (email.includes('@')) ? 'Valid email address' : 'Invalid email address';
console.log(message);



const arr = [];
const mess = arr.length > 0 ? 'Array has values' : 'Array is empty';
console.log(mess);

